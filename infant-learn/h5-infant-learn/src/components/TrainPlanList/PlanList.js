import React from 'react'
import {PullRefreshWrappedComp, NoAccordionCourse} from '../index'

const PlanList = (props) => <NoAccordionCourse  {...this.props} />

export default PullRefreshWrappedComp()(PlanList)
